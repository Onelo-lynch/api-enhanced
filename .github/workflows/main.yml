name: Keep Vercel API Alive

on:
  schedule:
    - cron: "*/5 * * * *"  # 每 5 分钟 ping 一次 API
  workflow_dispatch:        # 允许手动触发

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the API to keep it awake
        run: curl https://api.onegrow.xyz/personalized
